<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-5.0.xsd"
        objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">

    <changeSet id="20260428-1-add-chat-room-invite-fields" author="copilot">
        <addColumn tableName="chat_rooms">
            <column name="invite_token" type="VARCHAR(255)"/>
            <column name="invite_rotated_at" type="DATETIME"/>
            <column name="invite_enabled" type="BOOLEAN" defaultValueBoolean="true"/>
        </addColumn>

        <update tableName="chat_rooms">
            <column name="invite_enabled" valueBoolean="true"/>
            <where>invite_enabled IS NULL</where>
        </update>

        <sql dbms="postgresql">
            UPDATE chat_rooms
            SET invite_token = md5(room_id::text || '-' || extract(epoch from clock_timestamp())::text || '-' || random()::text)
            WHERE invite_token IS NULL;
        </sql>

        <addNotNullConstraint tableName="chat_rooms" columnName="invite_token" columnDataType="VARCHAR(255)"/>
        <addNotNullConstraint tableName="chat_rooms" columnName="invite_enabled" columnDataType="BOOLEAN"/>
        <addUniqueConstraint tableName="chat_rooms"
                             columnNames="invite_token"
                             constraintName="uk_chat_rooms_invite_token"/>
    </changeSet>

</databaseChangeLog>
